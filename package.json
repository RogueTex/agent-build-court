{
  "name": "agent-build-court",
  "version": "0.1.0",
  "private": true,
  "description": "A no-login static MVP that turns a rough idea into a spec-first build packet through a visible multi-agent debate.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "check": "node --check app.js && node scripts/smoke-test.mjs",
    "test": "npm run check"
  },
  "keywords": [
    "codex",
    "agent-committee",
    "multi-agent",
    "build-packet",
    "static-site"
  ],
  "license": "MIT"
}
